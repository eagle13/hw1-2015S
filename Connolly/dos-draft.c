#include <stdio>
#include <string>

int main()
{
  //Change the source to read input

  //getline
  //Increment through a line, keeping track of the current character and the character behind it.
  //Once the first character = \n, delete the character behind it!
  
  //getchar() = char a 
  while (a != EOF)
  {
    while (a != '\n')
    {   
      char b = a;
      getchar() = a;
      }
   } 
    //delete char b

   */
  return 0;

}
